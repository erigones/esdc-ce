Instalation for scss development
================================

In Fedora Linux:

        sudo dnf install rubygems ruby-devel
        sudo gem update --system
        sudo gem install compass
        cd  $erigones_home/gui/static/gui
        compass watch
