"""
Danube Cloud scripts and binaries

This is also a Python module mostly because the ``bin.eslib`` modules can be imported and
used by api or que functions (e.g. ``bin.eslib.qga.COMMANDS`` used from the ``vm_qga`` API view.
"""
